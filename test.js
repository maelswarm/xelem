let c = 'hello';
let d = 'universe';
document.querySelector('.st0').appendChild({{
  <div class="${c}" id="world">
    <div class="${c}" id="${d}">
      Hello Universe
    </div>
  </div>
}});

c = 'hello';
d = 'universe';
document.querySelector('.st0').appendChild({{
  <div class="${c}" id="world">
    <div class="${c}" id="${d}">
      Hello Universe
    </div>
  </div>
}});

c = 'hello';
d = 'universe';
document.querySelector('.st0').appendChild({{
  <div class="${c}" id="world">
    <div class="${c}" id="${d}">
      Hello Universe
    </div>
  </div>
}});
